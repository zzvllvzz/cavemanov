---
name: cavemanov-review
description: >
  Однострочные комментарии code review. Используй когда пользователь просит
  провести review, проверить код, или вызывает /cavemanov-review.
---

Проверь текущие изменения кода. Формат — однострочные замечания.

## Формат

Одна строка на замечание:
```
L<номер_строки>: <severity> <проблема>. <фикс>.
```

Severity:
- `bug` — сломано, не работает как задумано
- `risk` — работает, но опасно (гонка, утечка, безопасность)
- `nit` — стилистика, мелочь
- `q` — вопрос, нужно уточнение

## Правила

- Пропускать похвалу — "хорошо написано", "отличная идея" не нужны
- Пропускать очевидное — `missing semicolon`, `trailing whitespace` — работа линтера
- Если код норм — сказать `LGTM` и остановиться
- Максимум одна строка на замечание; если нужно больше — разбить на несколько замечаний
- Ссылка на конкретную строку обязательна

## Примеры

Хорошо:
```
L42: bug off-by-one в цикле, `i <= arr.length` → IndexError. Использовать `<`.
L78: risk `JSON.parse` без try/catch — краш на невалидном input.
L104: nit магическая константа `86400`, заменить на `SECONDS_PER_DAY`.
L130: q зачем retry с delay=0? Это busy loop.
```

Если всё чисто:
```
LGTM
```

Плохо:
```
L42: Хорошая функция, но можно улучшить. Может быть стоит подумать о том чтобы...
```

## Процесс

1. Прочитать diff (`git diff` или указанный файл)
2. Для каждой проблемы — одна строка в формате выше
3. Отсортировать по серьёзности: bug → risk → nit → q
4. Если проблем нет — `LGTM` и стоп
